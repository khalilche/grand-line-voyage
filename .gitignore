node_modules/
server/node_modules/
dist/
.env
*.log

# scratch test artifacts (Playwright screenshots + throwaway probe scripts)
server/_*.png
server/*-test*.mjs
server/phx*.mjs
server/*-tmp.mjs

# Heavy raw authoring material — kept locally, not needed to build or run the
# game (nothing in src/ imports from here). Excluded so the repo stays small
# enough to clone/deploy fast and under GitHub's file-size limits.
tools/pk-particles/
tools/models-src/
tools/shots/

# intermediate art gen output
tools/_*.svg
